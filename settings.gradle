plugins {
    // JDK toolchain auto-provisioning — 글로벌 JDK 설치 없이 Gradle 이 Adoptium 등에서 자동 다운로드.
    // build.gradle 의 `JavaLanguageVersion.of(21)` 과 짝을 이뤄 작동.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'shield'
